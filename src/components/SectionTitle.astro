---
const {
  eyebrow,
  title,
  text,
  align = 'left',
} = Astro.props;
---

<div class={`section-title ${align === 'center' ? 'section-title-center' : ''}`}>
  {eyebrow && <p class="eyebrow">{eyebrow}</p>}
  <h2>{title}</h2>
  {text && <p class="lead">{text}</p>}
</div>